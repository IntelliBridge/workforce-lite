apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards
spec:
  selector:
    app: opensearch-dashboards
  ports:
  - port: {{ .Values.opensearchDashboards.service.port }}
    targetPort: 5601